---
import { Icon } from 'astro-icon/components'

interface Props {
  label: string
  to?: string
  outline?: boolean
  go?: boolean
  newTab?: boolean
}

const { label, to, outline = false, go = false, newTab = false } = Astro.props

const Element = to ? 'a' : 'button'
---

<Element href={to} class="button" class:list={{ outline }} target={newTab ? '_blank' : undefined}
  >{label}{!!go && <Icon name="mdi:arrow-right" />}</Element
>

<style lang="scss" is:global>
  @use '../styles/charter';
  @use '../styles/text-styles';

  .button {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    text-decoration: none;
    outline: none;

    @include text-styles.display2SemiBold;
    padding: 14px 18px;
    gap: 3px;
    border-radius: 6px;
    line-height: 10px;

    background: charter.$secondary500;
    color: charter.$neutrals50;
    border: solid 1px charter.$secondary500;

    &.outline {
      color: charter.$secondary500;
      background: charter.$neutrals50;
    }

    > [data-icon] {
      font-size: 16px;
      margin: -3px 0 -3px 3px;
    }
  }
</style>
